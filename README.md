# test-linear

This repository uses GitHub Pages for a remote preview deployment.

## Preview deployment

- Workflow: `.github/workflows/deploy-preview.yml`
- Triggered on pushes to `main` and `PAN-18-deploy-remote-environment`
- Manual trigger available via **Run workflow** in GitHub Actions

After a successful run, the preview URL is available in the workflow job under the `github-pages` environment.
